json.extract! @review, :biz_id

# json.partial! 'review', review: @review
# if user
#   login(user)
#   render :json => {fname: user.fname, lname: user.lname}
# else
#   render :json => {
#     :errors => "Incorrect email or password" }, :status => 422
# end
